<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- The relay cache is disposable; the session pointer is not secret (npub only). -->
    <exclude domain="database" path="athena.db" />
</full-backup-content>
